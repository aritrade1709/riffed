# Single-package repo. The root is listed explicitly because pnpm treats the
# presence of this file as declaring a workspace.
packages:
  - "."

# pnpm asks before running a dependency's install scripts. esbuild needs its one
# to unpack the platform binary that Vite compiles with.
allowBuilds:
  esbuild: true
