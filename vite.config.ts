import { defineConfig } from "vite";

// Relative base so the same build works on GitHub Pages project sites,
// a custom domain, or a local `vite preview`.
export default defineConfig({ base: "./" });
